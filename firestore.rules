rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Workstation transfer package snapshots (accessible without login)
    match /packages/{packageId} {
      allow read, write: if true;
    }
  }
}
